# Python 기초 과제 - 도서 관리 시스템

## 프로젝트 구조

```text
python_final_project_김지현/
├── models/
│   ├── __init__.py
│   ├── base_book.py
│   └── specialized_books.py
├── utils/
│   ├── __init__.py
│   └── helpers.py
├── main.py
├── pyproject.toml
└── uv.lock
```

## 실행 방법

프로젝트 폴더에서 다음 명령을 실행합니다.

```bash
uv sync
uv run python main.py
```

VS Code를 사용할 경우 `uv sync`로 생성된 `.venv`의 Python 인터프리터를 선택합니다.

## 구현 완료 항목

### 필수

- [x] `main.py`의 `while` + `input()` 기반 CLI 메뉴
- [x] 1. 도서 등록
- [x] 2. 전체 도서 조회
- [x] 3. 도서 검색
- [x] 4. 대여/반납 처리
- [x] 5. 종료
- [x] 부모 `Book` 클래스
- [x] 일반 단행본 `PrintedBook` 자식 클래스
- [x] 전자도서 `Ebook` 자식 클래스
- [x] 프라이빗 인스턴스 변수와 읽기 전용 property 활용
- [x] `show_info()` 메서드 오버라이딩
- [x] 도서 상세 정보 딕셔너리 활용
- [x] ISBN 중복 방지용 집합(set) 활용
- [x] 여러 도서 순서 보관용 리스트 활용
- [x] 중복 ISBN 및 공백 입력 차단
- [x] 자료구조 선택 근거를 코드 주석으로 기록
- [x] ISBN 기반 대여/반납 상태 전환
- [x] 숫자 메뉴 입력의 `ValueError` 예외 처리
- [x] 잘못된 입력에도 프로그램이 종료되지 않도록 입력 검증
- [x] 반복 기능을 함수와 모듈로 분리
- [x] `models`, `utils`, `main.py` 역할 분리
- [x] Python 문법 컴파일 검사
- [x] 등록 → 조회 → 검색 → 대여 → 반납 → 통계 → 종료 시나리오 테스트
- [x] 문자 메뉴 입력, 중복 ISBN, 존재하지 않는 ISBN 예외 시나리오 테스트

### 심화

- [x] 대여/반납 이력을 `(ISBN, 처리유형, 처리시간)` 튜플로 저장
- [x] 이력 튜플을 리스트에 순차 저장
- [x] 통계 조회 메뉴 추가
- [x] 현재 월 대여 건수 계산
- [x] 가장 많이 대여된 도서 집계
- [x] 존재하지 않는 도서 및 잘못된 상태의 대여/반납 방어
- [x] 입력 단계의 유효성 검증 강화
- [x] 디버깅/재발 방지를 위한 처리 근거와 자료구조 선택을 주석으로 기록

## 자료구조 선택 근거

- `list`: 도서 객체와 대여 이력을 입력된 순서대로 유지하며 반복 조회하기 위해 사용합니다.
- `dict`: ISBN을 key로 도서의 상세 정보를 key-value 구조로 관리하기 위해 사용합니다.
- `set`: ISBN은 중복되면 안 되므로 고유값만 저장하고 중복 여부를 빠르게 확인하기 위해 사용합니다.
- `tuple`: 이미 발생한 대여/반납 이력 한 건을 이후에 변경하지 않도록 묶기 위해 사용합니다.

## 객체지향 설계

- `Book`: 도서명, 저자, ISBN, 대여 상태를 관리하는 부모 클래스입니다.
- `PrintedBook`: `Book`을 상속받고 페이지 수를 추가합니다.
- `Ebook`: `Book`을 상속받고 파일 형식을 추가합니다.
- 공통 데이터는 부모 클래스가 관리하고, 도서 유형별 상세 출력은 자식 클래스의 `show_info()`에서 오버라이딩합니다.
- 주요 데이터는 `__변수명` 형태의 프라이빗 변수로 관리하고 `property`를 통해 읽습니다.

## 예외 처리 및 디버깅 확인 내역

1. 메뉴에 문자를 입력하면 `int()` 변환에서 발생하는 `ValueError`를 처리하여 다시 입력받습니다.
2. 빈 문자열이나 공백만 입력하면 입력 단계에서 다시 요청합니다.
3. 이미 등록된 ISBN을 다시 입력하면 `set`을 이용해 중복을 차단합니다.
4. 존재하지 않는 ISBN으로 대여/반납을 시도하면 상태 변경 없이 안내합니다.
5. 이미 대여 중인 도서를 다시 대여하거나, 대여되지 않은 도서를 반납하는 잘못된 상태 전환을 차단합니다.
6. 대여 상태 변경 뒤 상세정보 딕셔너리도 다시 생성하여 객체 상태와 저장된 상세정보가 어긋나지 않도록 수정했습니다.

## 제출 전 로컬 PC 최종 확인

아래 3개는 실제 제출에 사용할 PC에서 수행합니다.

- [ ] `uv sync` 실행 후 `.venv` 생성 확인
- [ ] VS Code에서 해당 `.venv` Python 인터프리터 선택 후 `main.py` 실행 확인
- [ ] 완성된 `python_final_project_김지현/` 폴더 전체를 `python_final_project_김지현.zip`으로 압축하여 LMS 제출
